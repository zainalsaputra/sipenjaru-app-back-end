<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
            box-sizing: border-box; /* Include padding in width/height calculations */
        }
        .container {
            max-width: 800px;
            width: 100%;
            text-align: center;
        }
        h1 {
            color: #333;
        }
        p {
            font-size: 1.2em;
            color: #666;
        }
        .small-text {
            font-size: 0.9em;
            color: #888;
        }
        .link {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }
        .link:hover {
            background-color: #0056b3;
        }
        @media (max-width: 600px) { /* Smartphone (portrait) */
            p {
                font-size: 1em;
            }
            .small-text {
                font-size: 0.8em;
            }
            .link {
                padding: 8px 16px;
            }
        }
        @media (min-width: 601px) and (max-width: 1024px) { /* Tablet */
            p {
                font-size: 1.1em;
            }
            .small-text {
                font-size: 0.85em;
            }
            .link {
                padding: 10px 20px;
            }
        }
        @media (min-width: 1025px) { /* Laptop and desktop */
            body {
                padding: 40px;
            }
            p {
                font-size: 1.4em;
            }
            .small-text {
                font-size: 0.9em;
            }
            .link {
                padding: 15px 25px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1><%= title %></h1>
        <p>Welcome to our amazing SIPENJARU Application!</p>
        <p class="small-text">This API is a prototype in the development stage</p>
        <i class="fas fa-smile-beam" style="font-size: 50px; color: #ffcc00;"></i>
        <br>
        <a href="#" class="link" id="api-docs-link">View API Documentation</a>
    </div>

    <script>
        document.getElementById('api-docs-link').addEventListener('click', (e) => {
            e.preventDefault();
            alert('Dokumentasi masih belum tersedia :D');
        });
    </script>
</body>
</html>
